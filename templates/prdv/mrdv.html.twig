{% extends 'base_proprietaire.html.twig' %}

{% block title %}Propriétaire{% endblock %}
{% block body %}
    <div id="hero" class="d-flex justify-content-center align-items-center">
        <div class="container position-relative" data-aos="fade-up" data-aos-delay="100">

            <table id="example" class="table table-striped" style="width:100%">
                <thead>
                <tr>
                    <th>Name</th>
                    <th>Pays</th>
                    <th>Gouvernorat</th>
                    <th>Ville</th>
                    <th>Rue</th>
                    <th>Consulter RDV</th>
                </tr>
                </thead>
                <tbody>
                <tr {% for a in veto %}>
                    <td>{{ a.name }}</td>
                    <td>{{ a.pays }}</td>
                    <td>{{ a.gouvernorat }}</td>
                    <td>{{ a.ville }}</td>
                    <td>{{ a.rue }}</td>
                    <td>

                        <form action="{{ path('lrdv', {'id_Veto': a.id})}}" method="post">
                            <div class="form-group">
                                <div class="col-sm-offset-3 col-sm-9">
                                    <button type="submit" class="btn btn-primary">Rendez-Vous</button>
                                </div>
                            </div>
                        </form>


                    </td>
                </tr {% endfor %}>


                </tbody>
            </table>
        </div>
    </div>










{% endblock %}
